# Admin Roles Configuration
# One role ID per line
# Only users with these roles can use /flights track and /flights relay commands
# Get role IDs by right-clicking a role in Discord and copying the ID

