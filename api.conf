# API Configuration
# This file contains custom API endpoints that will receive aircraft data
# Format: One URL per line, starting with http:// or https://
# The bot will POST all aircraft data (7500, 7700, and all tracked aircraft) to these endpoints

# Example:
# https://your-api.example.com/aircraft
# https://another-api.example.com/data
# http://localhost:3000/adsb

# Your API endpoints:
